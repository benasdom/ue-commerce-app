

// import { createSlice } from '@reduxjs/toolkit'

// // Remeber to filter out all duplicates before using useSelector for this array
// // Remeber to filter out all duplicates before using useSelector for this array
// // Remeber to filter out all duplicates before using useSelector for this array


// const sumcart = createSlice({
// name:"carttotal",
// initialState:{cartitem:[],val:0,completed:false},  
// reducers: {
// cartitemsadded:(state, action)=>{

// let forcart={
//   name:action.payload.name,
//   image:action.payload.image,
//   price:action.payload.price,
//   id:action.payload.id,
//   amount:action.payload.itemamount,
// }
// if(state.cartitem.push(forcart)){


//     for(let i=0;i<state.cartitem.length;i++)
//     {
//         state.cartitem[i].amount>0? parseInt(state.cartitem[i].amount)-1: state.cartitem.indexOf(state.cartitem[i])-1;
//         console.log(state.cartitem[i])
//     }
    
// }
// else{
//     false
// }

// },

// }
// })



// export const  {cartitemsadded} = sumcart.actions
// export default sumcart.reducer

// // Remeber to filter out all duplicates before using useSelector for this array
// // Remeber to filter out all duplicates before using useSelector for this array
// // Remeber to filter out all duplicates before using useSelector for this array
